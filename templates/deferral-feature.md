# DEFERRAL {{id}}: DESKTOP-{{issue}} — feature

- What: {{what}}
- Raised by: {{raisedBy}}
- Where: {{where}}
- Recorded: {{recordedAt}}
  <!-- Status is derived from the journal, not stored here —
       `pdkit defer list --issue {{issue}}`.

       Below the rule is the DRAFT of the follow-up issue, in the fields of
       podman-desktop's own `Feature 💡` form. Open the issue through the
       template in the GitHub UI and copy field by field, so the posted issue
       reads like every other feature request in the repository.

       Opening it is a human action: `gh issue create` is denied at the hook. -->

---

### Is your feature request related to a problem? Please describe

<!-- The problem, not the solution — the next field is for the solution. What a
     person cannot do today, and what it costs them. -->
{{problem}}

Raised in review on {{where}}:

> {{quote}}

{{url}}

### Describe the solution you'd like

<!-- Concrete enough to become a requirement at triage: what a person should see,
     not what the code should contain. -->
{{solution}}

### Describe alternatives you've considered

<!-- Including the ones rejected, with why. A reviewer who suggested one of them
     is owed the reason it was not taken, and a request that lists no
     alternatives reads as one that considered none. -->
{{alternatives}}

### Additional context

<!-- Screenshots go here. Each placeholder below is deliberately VISIBLE text
     rather than an HTML comment: a comment renders as nothing, so a forgotten
     one reaches the issue invisibly. If you post one unreplaced, it is obvious.

     To replace: drag the file into the GitHub comment box. -->
{{screenshots}}

{{additionalContext}}
